# Relational Enclosure / News State Alignment v0.1

**Status:** ACTIVE_REVIEW / EVIDENCE-BOUND ADAPTATION

News does not inherit physical ontology from the Holo Mesh. It can reuse the relation grammar for evidence-traced public-state analysis.

For a bounded story/event/state, preserve three co-present questions:

```text
difference
  what changed, diverged, accumulated, or became newly observable?

bounded state
  what entity/event/institutional configuration is actually supported by the evidence?

surround
  what upstream causes, downstream effects, exchanges, constraints, absences, exclusions, counterparties, and lost/unobserved information condition the read?
```

For two actors/events, compare typed relations rather than proximity alone:

```text
shared evidence
shared upstream cause
direct contribution
exchange
exclusion/constraint
common environment
projection/co-occurrence only
unknown
```

Locks:

```text
co-occurrence != coordination
shared projection != shared cause
contribution != culpability
zero net change != no opposing pressures
repeated citation lineage != independent evidence
```

A State Drive-style trace can preserve how a public state changed over time; Atlas-style comparison can preserve multiple evidence-qualified maps. Neither upgrades inference to fact.

Tiny lock:

```text
Track the state and the surround.
Separate causal crossing from projected overlap.
Preserve opposing pressures at balance.
Let evidence type the relation.
```
